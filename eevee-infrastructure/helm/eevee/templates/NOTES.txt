EEVEE has been deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Components enabled:
  postgres       : {{ .Values.postgres.enabled }}
  redis          : {{ .Values.redis.enabled }}
  scheduler-api  : {{ .Values.schedulerApi.enabled }}
  queue-worker   : {{ .Values.queueWorker.enabled }}
  frontend       : {{ .Values.front.enabled }}
  ingress        : {{ .Values.ingress.enabled }}
  cert-manager   : {{ .Values.certManager.issuer.enabled }}

Useful commands:
  kubectl -n {{ .Release.Namespace }} get pods
  kubectl -n {{ .Release.Namespace }} get ingress
  helm -n {{ .Release.Namespace }} status {{ .Release.Name }}

{{- if .Values.ingress.enabled }}

Configured hosts:
  API      : https://{{ .Values.ingress.hosts.api }}
  Frontend : https://{{ .Values.ingress.hosts.front }}
{{- if .Values.ingress.hosts.frontWww }}
  Frontend : https://{{ .Values.ingress.hosts.frontWww }}
{{- end }}
{{- end }}
