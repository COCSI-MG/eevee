apiVersion: v1
kind: ConfigMap
metadata:
  name: eevee-config
  labels:
    {{- include "eevee.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
{{- end }}
