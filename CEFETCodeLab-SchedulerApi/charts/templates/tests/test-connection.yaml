apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cefet-codelab-schedulerapi.fullname" . }}-test-connection"
  labels:
    {{- include "cefet-codelab-schedulerapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cefet-codelab-schedulerapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
