apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cefet-codelab-platform-api.fullname" . }}-test-connection"
  labels:
    {{- include "cefet-codelab-platform-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cefet-codelab-platform-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

