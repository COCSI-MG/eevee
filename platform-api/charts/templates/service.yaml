apiVersion: v1
kind: Service
metadata:
  name: {{ include "cefet-codelab-platform-api.fullname" . }}
  labels:
    {{- include "cefet-codelab-platform-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cefet-codelab-platform-api.selectorLabels" . | nindent 4 }}

