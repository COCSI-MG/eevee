apiVersion: v1
kind: Service
metadata:
  name: {{ include "cefet-codelab-schedulerapi.fullname" . }}
  labels:
    {{- include "cefet-codelab-schedulerapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cefet-codelab-schedulerapi.selectorLabels" . | nindent 4 }}
