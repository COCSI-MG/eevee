apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CEFETCODELab-Node-DefaultNode.fullname" . }}-test-connection"
  labels:
    {{- include "CEFETCODELab-Node-DefaultNode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CEFETCODELab-Node-DefaultNode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
